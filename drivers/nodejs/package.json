{
  "name": "@icedb/driver",
  "version": "0.1.0",
  "description": "Node.js driver for icedb",
  "main": "index.js",
  "scripts": {
    "build": "napi build --platform --release",
    "test": "node test.js"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.4"
  }
}
