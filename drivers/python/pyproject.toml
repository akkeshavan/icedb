[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "icedb"
version = "0.1.0"
description = "Python driver for icedb - a PostgreSQL-compatible RDBMS in Rust"
requires-python = ">=3.8"

[tool.maturin]
features = ["extension-module"]
